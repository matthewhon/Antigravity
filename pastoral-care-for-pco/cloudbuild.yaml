steps:
- name: 'gcr.io/cloud-builders/docker'
  dir: 'pastoral-care-for-pco'
  args: ['build', '-t', 'gcr.io/${_PROJECT_ID}/${_SERVICE_NAME}', '.']

- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/${_PROJECT_ID}/${_SERVICE_NAME}']

- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args:
    - 'run'
    - 'deploy'
    - '${_SERVICE_NAME}'
    - '--image'
    - 'gcr.io/${_PROJECT_ID}/${_SERVICE_NAME}'
    - '--region'
    - '${_REGION}'
    - '--platform'
    - 'managed'
    - '--allow-unauthenticated'
    - '--port'
    - '8080'
    - '--timeout'
    - '300s'
    - '--min-instances'
    - '1'
    - '--project'
    - '${_PROJECT_ID}'
    - '--update-env-vars'
    - 'CANVA_CLIENT_ID=${_CANVA_CLIENT_ID}'

options:
  logging: CLOUD_LOGGING_ONLY

substitutions:
  _PROJECT_ID: pastoral-care-for-pco
  _SERVICE_NAME: pastoral-care-for-pco
  _REGION: us-central1
  _CANVA_CLIENT_ID: 'OC-AZ9dHwB8GH1_'

images:
  - 'gcr.io/${_PROJECT_ID}/${_SERVICE_NAME}'
