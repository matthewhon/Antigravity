steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/${_BUILD_PROJECT}/${_SERVICE_NAME}', '.']

- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/${_BUILD_PROJECT}/${_SERVICE_NAME}']

- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args:
    - 'run'
    - 'deploy'
    - '${_SERVICE_NAME}'
    - '--image'
    - 'gcr.io/${_BUILD_PROJECT}/${_SERVICE_NAME}'
    - '--region'
    - '${_REGION}'
    - '--platform'
    - 'managed'
    - '--allow-unauthenticated'
    - '--port'
    - '8080'
    - '--timeout'
    - '300s'
    - '--project'
    - '${_DEPLOY_PROJECT}'

options:
  logging: CLOUD_LOGGING_ONLY

substitutions:
  _BUILD_PROJECT: pastoral-care-for-pco    # project where Cloud Build SA has push rights
  _DEPLOY_PROJECT: gen-lang-client-0779452267   # project where Cloud Run service lives
  _SERVICE_NAME: pastoral-care-for-pco
  _REGION: us-central1

images:
  - 'gcr.io/${_BUILD_PROJECT}/${_SERVICE_NAME}'
